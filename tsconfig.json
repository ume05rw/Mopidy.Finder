{
    // --------------------------------------------------
    // VisualStudioコンパイル用
    // --------------------------------------------------

    // 開発用をそのまま使用。
    "extends": "./tsconfig.dev.json"
}
